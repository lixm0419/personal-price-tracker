# Copy these names into your shell, CI secret store, or a local .env file.
# The application reads the process environment and does not load .env itself.
PRICE_TRACKER_EMAIL_USERNAME=your-smtp-username
PRICE_TRACKER_EMAIL_PASSWORD=your-smtp-password
